mixin comment( args )
	-
		args.name = ( args.name ) ? args.name : 'Иванов Иван';
		args.foto = ( args.foto ) ? src.img+args.foto : src.img+'user-xs.jpg';
		args.comment = ( args.comment ) ? args.comment : 'Lorem ipsum dolor sit amet, consectetur adipisicing elit.';
	.media.comments__item.item
		.media-left.pull-left: a(href="#"): img.lazy.media-object.avatar.hidden-xs(src="#", data-src=args.foto, alt=args.name)
		.media-body
			h4.media-heading.author #{args.name}
			p.comment #{args.comment}
			div.clearfix
				date.small.alignleft.text-info 20.05.2018
				.alignright
					.btn.btn-link(href="#", role="button") Ответить
			if ( block )
				block
//
// комментарии
#comments.comments
	hr.separeted
	.clearfix
		h3.alignleft Комментарии:
			.alignright.small.font-normal всего 4
	.media-list
		+comment( {} )
			+comment( {} )
		+comment( {} )
		+comment( {} )
	include ../forms/_comments
// end комментарии
//